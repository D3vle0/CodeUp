#include <stdio.h>
#include <string.h>
int main(){
    char search[91];
    int num[26], cnt=0;
    gets(search);

    for (int i=0;i<strlen(search);i++){
        if (search[i]>=97 && search[i]<=122)
            num

    }
}