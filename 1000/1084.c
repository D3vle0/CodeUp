#include <stdio.h>
int main()
{
    int a, b, g;
    int i,d,k;
    scanf("%d %d %d", &a, &b, &g);
    for(i=0; i<a; i++){
     for(k=0; k<b; k++){
     for(d=0; d<g; d++){
     printf("%d %d %d\n", i, k, d);
      
}
}
}
printf("%d",a*b*g);
}